node_modules/
dist/

# Raw per-side captures. Regenerated by `npm run fidelity`; the committed evidence is the
# side-by-side composites and the report next to them.
fidelity/shots/

# Claude Design sync: the staged converter and the bundle it emits are both
# regenerated by `.ds-sync/package-build.mjs`. What is committed is the input —
# `.design-sync/` (config, the authored previews, the doc-category stubs).
.ds-sync/
ds-bundle/
